<IfModule mod_rewrite.c">
    RewriteEngine On
    
    # Dynamic RewriteBase - works for both local (/teacher-eval/) and production (/)
    RewriteCond %{HTTP_HOST} ^localhost [NC,OR]
    RewriteCond %{HTTP_HOST} ^127\.0\.0\.1 [NC]
    RewriteRule ^(.*)$ - [E=REWRITE_BASE:/teacher-eval/]
    
    RewriteCond %{ENV:REWRITE_BASE} ^$
    RewriteRule ^(.*)$ - [E=REWRITE_BASE:/]
    
    RewriteBase %{ENV:REWRITE_BASE}

    # Handle OPTIONS requests (preflight)
    RewriteCond %{REQUEST_METHOD} ^OPTIONS$ [NC]
    RewriteRule ^(.*)$ - [L]

    # Don't rewrite if it's a real file
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^(.*)$ - [L]
    
    # Don't rewrite if it's a real directory
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^(.*)$ - [L]

    # IMPORTANT: Handle dynamic API requests like /api/teachers/{id}.php
    # These don't match real files, so rewrite them to the actual API file
    RewriteRule ^api/([a-z-]+)/[a-f0-9]{24}\.php$ api/$1.php [QSA,L]
    
    # Allow direct requests to actual API files (POST/PUT/DELETE/GET with specific ID)
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^(.*)$ - [L]
    
    # Route everything else to index.php
    RewriteRule ^(.*)$ index.php?request=$1 [QSA,L]
</IfModule>
